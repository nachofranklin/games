will have a sprite2d child

a script attached with export var for
- piece resource
- grid_pos

funcs

setup piece (resource, pos, world_pos)
- sets the piece resource var
- sets the grid pos var
- sets the world pos
- sets the sprite img to the img saved to the resource

moving a piece (new pos, world pos)
- sets the grid pos
- sets the world pos


should i be setting the size here too or is that in game state?