color rect

var
- pos
- size
- colour
- selected colour
- selected

func
- set the tile values from board export var
- ready func (sets the color rect values)
- on tile clicked then emit a signal
- if a tile is selected
- if a tile is unselected